/**
 * 主题常量（T3 治理）：Element Plus 默认主色的唯一来源。
 *
 * 背景：`#409EFF` 原先散落在 store 初始化 / 站点配置兜底 / 响应式配置 / 主题模式复位 4 处，
 * 调整默认主色需全仓搜索且易漏；统一在此定义（颜色值大小写不敏感，采用小写口径）。
 */
export const DEFAULT_EP_THEME_COLOR = "#409eff";
