# E2E 测试说明

## 运行

| 命令                                                         | 说明                                                                                                                                                                                                                                                                                                                                                         |
| ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `pnpm test:e2e`                                              | 全量（双浏览器 chromium+webkit，dev push 之外的本地验证）                                                                                                                                                                                                                                                                                                    |
| `npx playwright test e2e/xxx.e2e.ts`                         | 只跑单个 spec（**不要**写成 `pnpm test:e2e -- <spec>`：追加参数实测不生效，会跑全量）                                                                                                                                                                                                                                                                        |
| `pnpm test:e2e:smoke`                                        | 只跑 `@smoke` 用例 + chromium（快速反馈，约 30s）                                                                                                                                                                                                                                                                                                            |
| `pnpm test:e2e:fresh` / `test:e2e:smoke:fresh`               | **先杀掉 18896/8848 旧进程再跑**（见下）                                                                                                                                                                                                                                                                                                                     |
| `pnpm test:e2e:parallel`                                     | 并行分片全量（默认 4 路，`E2E_PARALLEL` 可调；每路独立端口 + 独立 sqlite 库，~4min）。CI 全量档用 3 路 + `--reporter=list`。**注意**：起手会清理本分片段端口，串行跑批运行中勿再执行                                                                                                                                                                         |
| `pnpm test:e2e:perf` / `:perf:update`                        | 体验基线采集（chromium，TTFB/FCP/LCP/CLS；`:update` 写基线）；见「体验基线与时长预算」                                                                                                                                                                                                                                                                       |
| `pnpm test:e2e:csp`                                          | 页面层 CSP 隔离验证（构建产物 + **强制** CSP 头 + **双浏览器**：默认 `E2E_CSP_TLS=1` HTTPS 形态——openssl 自签 + ignoreHTTPSErrors，webkit 可收 Secure Cookie；核心页零违规 + `/__csp_probe` 负对照）；脚本内先 `pnpm build`                                                                                                                                  |
| `pnpm test:e2e -- e2e/tag-center.e2e.ts e2e/task-log.e2e.ts` | 标签中心闭环（新建 → 打标 → 标签列渲染 → 搜索区标签过滤 → 引用保护）；任务日志（执行记录一行一条 → 记录类型过滤 → 重跑 → 日志弹窗）                                                                                                                                                                                                                          |
| `pnpm test:e2e -- e2e/system-task.e2e.ts`                    | 定时任务闭环（立即执行/批量执行 → 实时日志弹窗）+ 执行历史（记录状态与触发人 → 行删除 / 批量删除，入口为任务日志）                                                                                                                                                                                                                                           |
| `npx playwright test e2e/role-permission.e2e.ts`             | 角色菜单权限配置树（渲染与统计 → 搜索过滤 → 全选/反选 → 三态标识 → 父子联动；闭环用例走「勾选 → 保存 → 重新打开回显」，临时角色自建自删保证幂等）                                                                                                                                                                                                            |
| `pnpm test:e2e -- e2e/table-sort.e2e.ts`                     | 表头排序（排序 caret → `ordering=field` / `-field` 请求）+ 高级筛选（受控 lookup `field__lookup=value` 过滤与回显；字段 → 条件 → 取值联动：性别 → 等于 → 男）                                                                                                                                                                                                |
| `pnpm test:e2e -- e2e/saved-view.e2e.ts`                     | 我的视图闭环（保存当前筛选 → 条件摘要 → 用当前筛选更新 → 一键套用 → 删除）；视图名带时间戳并在用例末尾自清理（双浏览器共享库幂等）                                                                                                                                                                                                                           |
| `pnpm test:e2e -- e2e/passkey.e2e.ts`                        | Passkey 注册闭环（**CDP 虚拟认证器**：`navigator.credentials.create` 真实仪式 → 服务端验签 → 列表回显 → 删除）。**仅 chromium**（虚拟认证器依赖 CDP，webkit 自动 skip）                                                                                                                                                                                      |
| `pnpm test:e2e -- e2e/data-permission.e2e.ts`                | 数据权限配置页（抽屉式新增/编辑）：行内规则编辑（过滤对象 `system.userinfo.creator` → 取值方式「跟随当前用户」）→ 规则摘要 → 未保存草稿试算 → 保存 → 列表生效范围与组合模式标签 → 编辑回显。定位靠 `data-testid`（`rule-add` / `rule-table` / `rule-field` / `rule-type` / `rule-value` / `rule-save` / `rule-row` / `scope-trigger`），不依赖文案与下拉序号 |
| `pnpm test:e2e -- e2e/theme-dark.e2e.ts`                     | 暗色模式主题一致性（走「项目设置」面板切换整体风格）：核心页在深色下必须是深表面 + 浅文本、切回浅色恢复；账户设置侧栏激活项在两态下都是白字。**主题是服务端站点配置**，用例开头归一浅色、结束（含 `afterEach`）还原，见下节陷阱                                                                                                                              |
| `pnpm test:e2e -- e2e/batch-update.e2e.ts`                   | 批量更新通用弹窗（角色列表为入口）：勾选行 → 工具栏「批量更新」→ 选字段 → 保存 → 接口落库 + 列表回显（该弹窗为 5 页共用组件，表单契约 `getPayload()`）                                                                                                                                                                                                       |

环境：sqlite 文件库（tmp/e2e.sqlite3）+ 进程内 FakeRedis + eager celery + 种子脚本（`scripts/e2e_seed.py`），
零外部服务依赖，不触碰本机 config.yml。配置见 `playwright.config.ts`（`E2E_API_PORT` 等
环境变量说明在文件头注释）。

### 独立检出运行（非多仓工作区）

默认假定 `xadmin-server` 与本仓库同级（`../xadmin-server`）。单独 clone 前端时用
`E2E_SERVER_DIR` 指向后端检出目录（playwright 会以 `tests.settings_e2e` 自动拉起后端与种子）：

```shell
XADMIN=../xadmin-server                                   # 或任意路径
XADMIN_SERVER_DIR=$XADMIN pnpm test:e2e:smoke             # 变量名为 E2E_SERVER_DIR*
E2E_SERVER_DIR=$XADMIN pnpm test:e2e
# 后端解释器默认 ${E2E_SERVER_DIR}/.venv/bin/python，可用 E2E_PYTHON 覆盖
```

> *正确变量名：`E2E_SERVER_DIR`（见 `playwright.config.ts` 文件头注释的完整清单）。
> 后端仓库需先装依赖：`uv sync --all-groups`（无 uv 时 `pip install -r requirements.txt -r requirements-dev.txt`）。

## ⚠️ reuseExistingServer 陷阱：改动后端代码后必须 fresh

`playwright.config.ts` 的 `reuseExistingServer: !process.env.CI` 会在端口已有服务时
**直接复用旧进程并跳过种子**。这意味着：

> 改了 `xadmin-server` 后端代码（模型/视图/迁移/配置）后，直接跑 `pnpm test:e2e`
> 复用的是**改动前启动的旧后端进程**——新行为不生效、测试结果与代码不符，
> 且极难排查（表现为"代码明明改了但接口行为还是旧的"）。

- 防护：改完后端后用 `pnpm test:e2e:fresh`（或 `test:e2e:smoke:fresh`），
  脚本先 `kill` 18896/8848 旧进程，再由 webServer 重新拉起 + 重新种子；
- 纯前端改动不受影响（vite dev 自带热更新），可放心用 `test:e2e`；
- CI（`CI=1`）下 `reuseExistingServer` 强制关闭，无需关心此问题；
- 历史案例：N3 功能迭代（2026-09-09）中 DataDict 序列化器修复后，复用旧进程
  导致 E2E 两次假失败（400 行为与已修复代码不符）。

## ⚠️ 列表断言陷阱：不要依赖「目标行在第一页」（曾误判为 webkit flaky）

RePlusPage 列表**固定发 `ordering=-created_time` 且默认 `pageSize=15`**（见
`src/components/RePlusPage/src/utils/hook.tsx`）。E2E 一轮里靠前的用例会持续创建带时间戳的
账号（`e2e_sync_*` / `e2e_imp_*` / `e2e_hist_*` 等），于是**早期种子账号（`xadmin`、
`e2e_user`）会被挤出第一页**：

- 表现：`locator(".el-table__row", { hasText: "xadmin" })` 直接「element not found」；
- 为什么只在 webkit 出现：webkit 是**第二个执行的浏览器阶段**，此时库里用户累积最多；
- 为什么看起来像 flaky：隔离重跑（只跑这几个 spec，创建的用户少）就绿了 —— 于是一度被
  登记为「webkit 时序 flaky」。**真实原因是测试数据污染 + 分页边界，不是时序抖动**
  （2026-09-10 定位：同一进程内全量跑必现、隔离跑必绿；直接调 `/api/system/user` 接口
  200 正常、total 已超 pageSize）。

**处置（断言纪律）**：

1. 需要定位具体行时，**先按条件过滤再断言**：
   `helpers.openList(page, "/system/user/index", { placeholder: "请输入用户名", value: "xadmin" })`
   （导航进页面后在搜索区填入并点「搜索」）。
   注意**不要**用 `?username=xadmin` 这种 route.query 预置——RePlusPage 会把 route.query
   合并进 searchFields 并**回填搜索框**，但**首屏列表请求并不带该条件**（实测：搜索框显示
   xadmin、表格仍是未过滤的第一页），必须在页面上真实触发一次搜索；
2. 只验证「列表能加载」时，断言**已渲染出数据行 + 分页可见**，不要绑定具体账号；
3. 弹层内选择器（`api-search-user`）同理：等目标行渲染后按需退化到首行，并等
   `.el-loading-mask` 等浮层消失再点击（否则点击被 `intercepts pointer events` 拦截）。

## ⚠️ 禁止固定延时：一律用 web-first 断言（`waitForTimeout` 已清零）

`page.waitForTimeout(ms)` 是**盲等**：快机器白等、慢机器照样挂，且掩盖真实原因。
除下表的例外外，所有「等一会儿再看」都必须换成 Playwright 的自动重试断言：

| 场景                       | 不要用                               | 改用                                                                                   |
| -------------------------- | ------------------------------------ | -------------------------------------------------------------------------------------- |
| 等元素出现 / 下拉展开      | `waitForTimeout(800)`                | `await expect(locator).toBeVisible({ timeout })`                                       |
| 等元素消失 / 弹层关闭      | `waitForTimeout(300)`                | `await locator.waitFor({ state: "hidden" })`                                           |
| 等折叠菜单展开             | `waitForTimeout(300)`                | 等**下一级子元素可见**（`el-menu` 无 `aria-expanded`，但折叠时子级不可见）             |
| 等路由切换                 | `waitForTimeout(300)`                | `await page.waitForURL(...)`（或重试点击后以此断言）                                   |
| 等异步结果变为可接受       | `waitForTimeout(1100)`               | `await expect.poll(async () => …, { intervals }).toBe(期望值)`                         |
| 等保存 / 校验路径稳定      | `waitForTimeout(1500)`               | 竞速「弹层关闭」与「校验报错出现」，**只在真被拦下时**补点一次                         |
| 等瞬时消息（`el-message`） | 裸 `locator(".el-message--success")` | 加 `.last()` 收敛到最新一条（上一条还在淡出时会命中 2 个元素 → strict mode violation） |

**唯一例外（服务端时间域约束）**：`online.e2e.ts` 的强制下线恢复用例。服务端按
`float(iat) <= float(revoked_at)` 判定被踢（`common/core/auth.py`），新签发 token 的
`iat` 必须跨过失效秒 —— 这不是 UI 状态，任何 DOM 断言都无法表达，故用
`expect.poll` 轮询「签发 → 以新 token 访问 userinfo」代替盲等（既拿到确定性，也不白等）。

## ⚠️ 主题 / 站点配置类用例会污染同库其它 spec

整体风格（浅色 / 深色 / 自动）与「灰色模式」等属**服务端站点配置**（`WEB_SITE_CONFIG`，
改动即防抖 PATCH），本地 `localStorage` 只是即时层：

- **不要直接写 localStorage 改主题**：布局在挂载与 resize 时会把内存态回写存储，
  外部写入会被覆盖（实测「写存储 → 刷新」不生效）；正确路径是走顶栏「项目设置」面板
  （`.set-icon` → `.right-panel` 内的 `.pure-segmented`），并等待
  `PATCH /api/system/configs/WEB_SITE_CONFIG` 落库（跨设备/刷新恢复依赖它）；
- 这类用例必须**开头归一**（先切回默认态）+ **结束还原**（含 `afterEach` 兜底），
  否则会打穿 `visual.e2e.ts` 基线（底色敏感）与其它页断言；参考 `theme-dark.e2e.ts`；
- 账户设置等页面不渲染主布局（无顶栏、无侧栏），切主题前需先 `page.goto("/#/welcome")`；
- 语言同理：切英文会破坏其它 spec 的中文定位，语言链路只在单测覆盖
  （见 `site-setting-live.e2e.ts` 头注释）。

## 视觉回归（`visual.e2e.ts`）

核心页（welcome / 用户 / 菜单 / 仪表盘 / AI / 聊天室）全页像素比对，为样式类改动（如 T2 `!important` 存量清零）提供安全网：

| 场景                    | 命令                                                                                                                   |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 本地比对（darwin 基线） | `pnpm test:e2e:visual`（按需，不进常规全量；默认双浏览器）                                                             |
| 本地更新基线            | `pnpm test:e2e:visual:update`                                                                                          |
| 生成 ubuntu 基线        | 手动触发 **Visual Baseline** workflow → 下载 `visual-baseline` artifact → 解压到 `e2e/visual.e2e.ts-snapshots/` 并提交 |

- 快照按平台后缀存放（`*-chromium-linux.png` / `*-chromium-darwin.png`），**跨平台像素必然差异**：ubuntu 基线只能在 CI 生成，本地 darwin 基线仅供本地比对；
- `e2e.yml` 的 `Visual Regression` job 与 full 同档（PR / main / 夜间 / 手动）跑 chromium 单浏览器；**基线未入库时自动跳过**（notice 提示先生成基线），入库后自动启用常态比对；
- 判定 `maxDiffPixelRatio 2%`（容忍字体渲染亚像素差）；stabilize 已禁 CSS 动画/光标闪烁并等 ECharts canvas 绘制结束（≥1.5s）；
- 失败产物见 `playwright-report-visual` artifact / `test-results/`（含 expected / actual / diff 三图）。

## 体验基线（`perf.e2e.ts`，U1）与跑批时长预算（Q4）

**体验基线**（首测 2026-09-17：登录页 LCP 1224ms / 列表页 CLS 0.05~0.24；**2026-09-18 列表页 CLS 收口至 0.0214**，根因与修复见文末陷阱表）：

| 场景              | 命令                                                   |
| ----------------- | ------------------------------------------------------ |
| 采集 + 与基线比对 | `pnpm test:e2e:perf`（chromium；无基线时只打印不比对） |
| 刷新基线          | `pnpm test:e2e:perf:update`                            |

- 指标：TTFB / FCP / LCP / CLS（PerformanceObserver 缓冲 LCP/layout-shift），页面稳定等待 `E2E_PERF_SETTLE`（默认 2s）；
- 基线文件 `e2e/perf-baseline.json` 按「平台-CI」分组（`darwin-local` / `linux-ci`），**跨环境数字不可比、只看同环境趋势**；
- 现阶段只做「离谱回归」兜底（TTFB ≤ 2s / LCP ≤ 5s / CLS ≤ 0.5）；正式预算待数据积累后评审；
- 采集口径为 dev 链路（vite + 种子后端），与生产构建存在差异，仅用于趋势与回归。

**跑批时长预算**（`scripts/e2e-parallel.mjs`）：每次跑批打印各 shard 与总用时，并按同环境基线
（`e2e/duration-budget.json`）校验 **+20% 上限**（`E2E_BUDGET_RATIO` 可覆盖）：

- 首次在某环境运行自动记录基线；`E2E_BUDGET_UPDATE=1` 强制刷新（如确认环境变更/合理增量）；
- 透传参数的子集运行不参与比对；跑批失败时不记录基线（避免把异常时长写成基线）；
- 超预算报错文案给出「本次 / 基线 / 上限」三值，便于判断是回归还是环境变化。

## 历史教训速查

| 教训                                                                                                                                                                                                                                                                                                                                                              | 处置                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 8896 与本机 compose nginx 端口冲突 → reuseExistingServer 误复用容器服务                                                                                                                                                                                                                                                                                           | 统一用 `test:e2e` 注入的 18896 隔离端口                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 改后端代码后复用旧进程 → 假失败                                                                                                                                                                                                                                                                                                                                   | `test:e2e:fresh` / `test:e2e:smoke:fresh`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| eager celery 下 `send_task` 不流转执行状态                                                                                                                                                                                                                                                                                                                        | dispatch 层走 `apply`（见 `system/views/task.py::_dispatch_periodic_run`）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| sqlite 并发 database is locked                                                                                                                                                                                                                                                                                                                                    | settings_e2e 已开 WAL + busy_timeout + IMMEDIATE                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 搜索区「展开」在部分断点是 **link** 而非 button（`getByRole("button", { name: /展开/ })` 找不到）→ 两种角色都试（`.or()`）                                                                                                                                                                                                                                        |
| EP notification（带操作按钮）不自动消失且拦截点击 → 先点 `.el-notification__closeBtn` 再操作                                                                                                                                                                                                                                                                      |
| E2E 下 `CELERY_TASK_ALWAYS_EAGER=1`：导出/导入同步跑完，**库内不存在 PENDING/RUNNING 窗口** → 取消按钮的 UI 断言不可写，取消语义放后端用例                                                                                                                                                                                                                        |
| RePlusPage 行操作按钮超出 `showNumber` 折叠进「更多」下拉 → 行内定位要先试直接点击，失败再展开下拉（`e2e/preview.e2e.ts` 的 `clickRowButton` 口径）                                                                                                                                                                                                               |
| 经 API 建用户时密码字段按密文协议解析（明文会回退默认密码）→ 断言只依赖用户名等非密码字段                                                                                                                                                                                                                                                                         |
| 敏感操作告警 WS 弹窗盖住抽屉按钮 → 点击持续 `element is not stable`                                                                                                                                                                                                                                                                                               | e2e_seed 用哨兵值关闭（`SENSITIVE_OPERATION_METHODS=["__E2E_DISABLED__"]`；**空清单=不按方法过滤=全告警**，且该配置走 SysConfig DB 值，settings 覆盖无效）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 断言目标行在「第一页」→ 全量跑越到后面越失败（只在 webkit 暴露，曾误判 flaky）                                                                                                                                                                                                                                                                                    | 用 `openList` 真实触发搜索过滤 / 断言「已加载出数据行」；详见上节「列表断言陷阱」                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 菜单点击后 hash 未生效（页面停在 welcome）→ 后续 `expect(table)` 报 element not found                                                                                                                                                                                                                                                                             | `openMenuPath` 点击后校验 hash，未生效则重开目录重试一次（见 helpers.ts 实现注释）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 机器负载高（IDE 满载 / 多浏览器并发）→ 10s 断言超时被击穿，基础用例也失败                                                                                                                                                                                                                                                                                         | 先看 `uptime`；失败行落在 login/导航/渲染等待处时按环境假失败处理，隔离重跑复核                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `waitForTimeout` 盲等（快机器白等、慢机器仍超时，掩盖真实原因）                                                                                                                                                                                                                                                                                                   | 全量改 web-first 断言（`toBeVisible` / `waitFor` 状态 / `expect.poll` 轮询结果）；仅服务端时间域约束（强制下线 iat）用 `expect.poll`（详见上节）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 登录页落到「当前服务器不允许登录」→ 找不到账号输入框（站点配置拉取失败/被拖慢，等价 config 为空分支）                                                                                                                                                                                                                                                             | `login()` 先等账号框可见；超时则 `reload()` 一次重新拉取配置再重试（不硬等、不重复提交）；同一页面内二次登录（登出后再登录）概率更高                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| a11y 扫描命中瞬态 color-contrast（el-tree 入场 opacity 未结束时 axe 把半透明文字与背景混色）                                                                                                                                                                                                                                                                      | `scanBlockingViolations` 先注入 `transition/animation: none` 样式让动画瞬移终态再扫（扩豁免清单是下策，勿绕过该函数直接扫）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 并行分片（`test:e2e:parallel`）高负载下登录 POST + 路由拉取可超 15s → `login()` 离开登录页断言被击穿                                                                                                                                                                                                                                                              | 断言上限放宽到 30s（auto-retry 上界）；仍失败交给 retries 兜底。注意：并行跑批的 `freePort` 起手会杀 18896/8848 段端口——**串行 E2E 运行中途不要再起并行跑批**，会把在跑的服务杀掉造成大面积假失败                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 并行分片共用 `./test-results` → 各 shard 启动阶段并发清理/写入同一目录，随机 shard「启动即失败」（Node 安全删除 shim 下直接 `FSMoveObjectToTrashSync` 抛错退出，易误判为用例失败）                                                                                                                                                                                | `e2e-parallel.mjs` 给每个 shard 传 `--output=test-results-shard-<i>` 隔离产物目录（2026-09-11 修复；串行跑批不受影响）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 数据权限页规则摘要断言与下拉选项文案不一致：取值方式在**摘要**里显示后端 `rule_meta` 的语义文案（如「目标用户本人」），而**下拉选项**的 label 来自 `KeyChoices`（如「本人ID」）                                                                                                                                                                                   | 断言按摘要口径写（`包含 创建人 目标用户本人`）；新增这类「同语义多文案」断言前先用 `textContent` 看过实际渲染，不要照抄选项 label                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 表单内多级弹层/抽屉里的自定义控件定位脆弱（下拉序号、label 文案随 i18n 变化）                                                                                                                                                                                                                                                                                     | 自定义控件挂 `data-testid`（挂在包裹元素而非 EP 内部 input 上——`ElInput` 的 `$attrs` 透传会让 `[data-testid=x] input` 失配），E2E 按 testid 定位                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 并行分片高负载下 `async-import` 的 `waitForEvent("download")` 30s 超时（仅 webkit，retries 内重跑即过）                                                                                                                                                                                                                                                           | **已治理（2026-09-17）**：下载等待统一走 `DOWNLOAD_TIMEOUT`（helpers.ts）——高负载档（`CI=1` / `E2E_PARALLEL>1`）放宽到 90s 并同步 `test.slow()`，本地串行仍 30s 以保真回归灵敏度                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 全量串行跑批（~17min）后段 webkit 多点失败：a11y/ai/analysis/dashboard/dform/webhook/oauth-im 同批出现（元素未稳定、60s 超时）                                                                                                                                                                                                                                    | 高负载环境假失败特征：**成批 + 跨模块 + 同一浏览器阶段**。隔离重跑对应 spec（`--project=webkit`）应全通过；真回归的特征是单 spec 稳定复现、且失败点固定在业务断言上（2026-09-13 两批实测：7 例、8 例隔离全绿）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 新增内置种子数据（loadjson 示例）后 a11y 扩面用例突然出现「列表从未有行」按钮的违规（如 icon-only 详情按钮无 aria-label 的 button-name critical）                                                                                                                                                                                                                 | 种子改变了 a11y 扫描的页面形态（列表从空到有行）：**隔离复跑仍失败 ≠ 回归，先看违规点是不是既有 UI 缺陷被数据首次暴露**（2026-09-13：审批/表单示例种子让流程定义列表有行，暴露 RePlusPage 详情按钮无可访问名，修 `aria-label` 而非回滚种子）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 隔离复跑时「login 卡 30s 仍停在 #/login」成批出现（连 a11y 登录页基线都挂）                                                                                                                                                                                                                                                                                       | 先确认 E2E 后端还活着（`curl 127.0.0.1:18896/api/common/api/health`）：fresh 跑完 webServer 已退出，`E2E_SEED=0 E2E_REUSE_SERVER=1` 复跑会复用已死/被污染的旧库；直接 `pnpm test:e2e:fresh`（或手动 kill 后 `E2E_API_PORT=18896 npx playwright test`）重建环境。另注意直跑 `npx` 不带 `E2E_API_PORT` 会撞 docker nginx 的 8896 端口（health 恰好命中导致种子被跳过）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 懒加载页签（`el-tab-pane :lazy`）未激活即不挂载 → 按 `.el-tab-pane` 索引定位面板随访问路径漂移（点过邮件/钉钉/飞书后，飞书是 nth(2) 而非 nth(3)）                                                                                                                                                                                                                 | 面板一律按页签名定位 `getByRole("tabpanel", { name })`，勿用 DOM 索引（notify-im 首跑即因此误判「飞书字段未渲染」，实际 DOM 正常）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 行操作按钮默认只渲染 3 个（`ButtonOperation` 的 `showNumber` 默认 3），第 4 个起折叠进「更多」下拉 → 直接 `getByRole("button", { name })` 点击恒超时（DOM 里根本没有该按钮）                                                                                                                                                                                      | 先 `hover` 该行的 `.el-dropdown` 展开下拉，再点 `.el-dropdown-menu__item`（change-history 的「变更历史」按钮即此情形，曾稳定失败）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 页面顶部统计面板内含 `el-table`（文件中心「占用最大的文件」）时，列表断言的 `.el-table__row` 会先命中面板里的行（按文件名 `hasText` 过滤同样命中）→ 主列表行断言恒失败                                                                                                                                                                                            | 统计面板内的数据列表用轻量自绘列表（文件中心 Top5 已如此），或定位时限定到主列表；给页面加面板后先数一遍 `.el-table` 数量                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Element Plus `el-input`（`type="textarea"`）不保证把 `data-testid` 透传到内部 `<textarea>` 上 → `[data-testid="x"] textarea` 一直等不到元素（页面其余断言全过，极易误判为「组件没渲染」）                                                                                                                                                                         | 需要子树定位时把 `data-testid` 挂**原生 wrapper div**；或直接 `getByPlaceholder()` / `getByRole()`。同类：icon-only 按钮（如抽屉开关）要显式 `aria-label`（2026-09-14 聊天室重构实测）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 权限门控页面（聊天室等按菜单权限点放行的页面）在 E2E 里不能用普通用户参与：`scripts/e2e_seed.py` 造的普通用户没有对应菜单授权 → REST 403 + WS 4403，表现与「后端没部署」极像                                                                                                                                                                                      | 多参与者用例统一用**两个超管**（`ADMIN` / `APPROVER`），页面权限本身由后端守护测试（种子权限点 path ↔ 真实路由）覆盖；新页面若要普通用户参与，需先给 e2e_seed 补菜单授权（2026-09-14）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Element Plus `el-switch` 的 `click` 是**切换**语义且根元素上**没有** `aria-checked`（在内部隐藏 checkbox 上）→ 对「可能已被前序用例打开的开关」直接 click 会把它**关掉**（双浏览器共享库时表现为同一用例跨浏览器 flaky，重试才过）                                                                                                                                | 封装 `ensureSwitchOn`：`sw.locator("input[type=checkbox]").isChecked()` 为 false 才 click（ai.e2e.ts 全局开关实测）；el-switch 上挂 `data-testid` 会透传到根元素，`getByTestId(...).click()` 可用                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 双浏览器（chromium+webkit）共享同一 E2E sqlite 库：先跑的浏览器留下的状态（开关已开/行已建/激活态已翻转）会让后跑浏览器按「初始态」写的断言失败 → 表现为 flaky，重试才过                                                                                                                                                                                          | 要么断言容忍多种合法状态（`expect(...).toPass` 轮询），要么行名/数据加随机后缀防唯一约束冲突；保存系统设置后立即断言生效不可靠（pub/sub 异步回写），留缓冲或 reload 再断言（2026-09-14 ai 档案页实测）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| a11y 登录页 color-contrast 首扫命中（chromium 首跑失败、retries 内重跑通过；共享库下 webkit 通常绿）                                                                                                                                                                                                                                                              | 登录页表单是**异步渲染**（站点配置拉取后才渲染 + Motion 入场动画）：采样时机不同会时而命中「忘记密码 / 登录按钮」等**既有的主题级低对比度**节点。2026-09-14 用干净 HEAD + 相同等待条件复核，命中节点完全相同 → 非回归；命中形态（`.is-(link/plain) > span`、`el-divider__text`）已按治理流程补进 `ALLOWED_VIOLATIONS` 并登记 `docs/accessibility-audit.md`（换全局主题色后摘除）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `split-pane` 拖拽断言在 webkit 稳定失败：`mouse.move` 大步移动的中间 mousemove 不足以触发 ReSplitPane 的位移判定（>2px）→ 被当作「点击重置」，左栏宽度回默认值 ≈60%（断言 35~45 失败）；chromium 通过                                                                                                                                                             | **既有 webkit 限制**（组件与用例均与 HEAD 一致，2026-09-18 上线前复核）：实测 steps 10→30、或「先小步再大步」会让 chromium 也开始失败——**不要动该用例的拖拽参数**；改动 `ReSplitPane` 或该 spec 后必须双浏览器验证                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `approval.e2e.ts`「待办角标归零」用例在与其他审批 spec **同库连跑**时失败（角标仍为 1，行已离开待我审批列表）                                                                                                                                                                                                                                                     | 角标口径是**全库 PENDING** 计数（不是本用例建的两单）：前序 spec（batch-export / chain / decision-info 等）留下的待审批单会顶住归零断言。判定回归前先**单独跑该 spec**（fresh 种子下通过即非回归）；要连跑则先清空库内待办或在用例内按本 run 建单隔离                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 聊天室公共房间历史消息持久保留 + 断言用宽正则（如 `/E2E 实时推送验证/`）→ 双浏览器共享库时后跑浏览器命中多条消息，`getByText` strict mode violation（webkit 稳定失败、隔离复跑才过）                                                                                                                                                                              | 断言精确到本次消息文本（含 `Date.now()` 后缀，如 notice-push 的 `getByText(text)`）；类比同页「共享库状态」模式的处置                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| EP `el-select` 选中/收起后下拉**仍以可见态残留在 DOM**（teleport 到 body，不 display:none）→ ①对后续同弹窗内其它 select 的点击被其拦截（报 `<span>xxx</span> intercepts pointer events`，拦截元素正是残留下拉的选项）；②`.el-select-dropdown:visible` 会命中多个下拉，`.first()` 拿到的是别的下拉的项                                                             | 同弹窗内连续操作多个 select：①每个 select 选完点一次弹窗空白区（如 `.el-dialog__header`）强制收起；②目标下拉经 combobox 的 `aria-controls` 精确锁定列表 `page.locator('[id="${listId}"] .el-select-dropdown__item')`（dashboard 卡片「可见角色」多选实测，2026-09-16）；③保存布局类操作后整行替换会重建按钮（detached），回显断言放在保存前读内存草稿                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 分析/集成系数据名唯一约束（Dataset/Dashboard/Report/Screen/WebhookSubscription.name `unique=True`）+ 双浏览器共享库 + 固定测试名（`E2E数据集`、`E2E外部系统` 等）→ 后跑浏览器创建同名必失败（曾误判负载瞬态）                                                                                                                                                     | 测试数据名加随机后缀（`E2E数据集-${Math.random().toString(36).slice(2, 8)}`）并以变量贯穿断言/下拉选择（2026-09-14 dashboard/analysis/webhook 实测）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 表单设计（dform）用例漏了上条处置：固定名 `E2E设备登记` → webkit 名称唯一约束冲突 → 保存失败弹窗不关（报 `not.toBeVisible` 误导性断言）；改名后提交内容断言 `page.getByText("device_name: E2E路由器")` 又命中 chromium/webkit 两条提交（strict mode violation）                                                                                                   | 名称加随机后缀；**表格行内的文本断言必须限定 scope**（`submissionRow.getByText(...)`）——共享库下的行断言与消息断言同理（2026-09-14 dform 实测，迁移 RePlusPage 后暴露）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| AI 动作用例（ai-action）：聊天室同房间历史卡片可见可点 + 按 `.last()` 取「最新卡片」→ 新卡片尚未渲染时命中历史卡片（其表单已被清理 → 执行必然失败，表现为 1001 但断言先行通过）；计数断言写死 `toHaveCount(0)` 也被历史卡片击穿                                                                                                                                   | 元素卡片按本 run 唯一标识 `hasText` 定位（随机表单名）；计数断言用「执行前基线 +1」而非绝对值；跨用例按用户隔离会话（双超管各一间 AI 房）（2026-09-15 实测）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| AI 动作 E2E 无真实 LLM：草稿生成需要确定性的模型回答                                                                                                                                                                                                                                                                                                              | playwright webServer 第三项拉起 `xadmin-server/scripts/stub_llm.py`（OpenAI 兼容桩，18897 端口，`test:e2e:fresh` 会清理）；用例以 API 建 AI 档案指向 `http://127.0.0.1:18897/v1` 并激活——同时实测了「多档案激活优先」通路（2026-09-15）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `pnpm <script> -- <args>` 透传时 `--` 是 **pnpm 的参数分隔符**：`-- --project=x` 是两个 `--` 叠写（分隔符 + 选项前缀），视觉上易被误读为多写/漏传（2026-09-15 实测讨论）                                                                                                                                                                                          | **位置参数无需 `--`**（`pnpm test:e2e:fresh e2e/chat.e2e.ts` 实测只跑该 spec：1.8m / 6 passed）；CI 与脚本里**直调** `pnpm exec playwright test <file> --project=x`（零歧义）；另：`--list` 不是零副作用验证（同样拉起 webServer）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 视觉回归（visual.e2e.ts）按需运行（`pnpm test:e2e:visual`，E2E_VISUAL=1）：全页像素比对，基线按平台目录存放（darwin/linux 像素必然差异）                                                                                                                                                                                                                          | 基线更新 `pnpm test:e2e:visual:update`；仪表盘等 ECharts 走 canvas 内部动画（CSS 禁不掉），stabilize 需等 ≥1.5s 再截图；CI 已接线（Visual Regression job + Visual Baseline workflow），ubuntu 基线入库后自动启用常态比对（2026-09-15）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| EP remote `el-select`（SuggestSelect，ADR-043）自动化输入三坑：①逐字符/`insertText`/`fill` 期间 EP 的 `states.inputValue` 在 debounce 窗口内被翻倍（remoteMethod 收到 `"xadminxadmin"`，搜索恒空）；②**展开中候选项的鼠标 click 不触发展开选中**（点击后 selected-item 仍显示旧值）；③旧弹窗（非 filterable）input 带 `readonly`，`fill` 的 editable 检查直接超时 | 输入用 `fill`（SuggestSelect 输入框非只读，单次 input 事件）；**选中用键盘导航** `press("ArrowDown") + press("Enter")`（走 `handleOptionSelect`，鼠标路径不可靠）；组件侧 `remoteMethod` 不信 EP 回调参数、直接读内部 `.el-select__input` 的 DOM 真值（两者对真实键入一致）；候选下拉仍按 `aria-controls` 精确锁定（2026-09-16 suggest.e2e 实测）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 页面层 CSP（nginx 托管 SPA）切强制前的隔离验证：dev 链路（vite 无 CSP 头）与 Report-Only 都测不出真实违规，「等真实流量观察零」在测试服不可达时无法累计                                                                                                                                                                                                           | 用 `pnpm test:e2e:csp`（`scripts/csp-page-server.mjs`：构建产物 + 强制头 + `/api`·`/media` 代理 + WS 透传 + `/__csp_probe` 负对照）。实测三处真实违规：① 生产产物 `index.html` 的内联 `window.process`（→ 抽成 `public/process-shim.js`）；② version-rocket 用 Blob 创建轮询 Worker（→ 策略加 `worker-src 'self' blob:`）；③ 菜单元数据 `ep:*` 图标触发 Iconify 在线拉取（→ **当天换成图标离线化**：常用图标随包注册`offlineIcon.ts` + 其余按 set 懒加载构建期内置图标集 `iconRegistry.ts`（同源 chunk，ep≈31KB/ri≈262KB/fa-solid≈212KB gzip，按需），`connect-src` 无外部主机；spec 同步断言「侧边栏菜单图标渲染」「图标选择器本地集渲染」）。另：生产构建的认证 Cookie 带 `Secure`（`src/utils/auth.ts` 的 `import.meta.env.PROD` 分支），**http 下 WebKit 拒收 → 无法登录**（chromium 视 loopback 为可信故可过）——**已收口（2026-09-18）**：跑批默认 `E2E_CSP_TLS=1`，验证服务以 HTTPS 提供（openssl 自签 SAN 含 localhost/127.0.0.1，webServer 与 context 双处 `ignoreHTTPSErrors`），**双浏览器（chromium+webkit）核心页零违规 + 探针命中**；坑：webServer 的健康检查独立于 context 的 `ignoreHTTPSErrors`，只配后者会探针 30s 超时（config.webServer 报 "Self-signed certificate detected"） |
| 列表页首屏「表格整体下移 / 表头忽然变高再回弹」的瞬态位移（`pnpm test:e2e:perf` 可复现，CLS 0.05~0.24 波动；`cls_top` 源=`el-table__body-wrapper`/`tr`）                                                                                                                                                                                                          | 两个同帧成因：①搜索列元数据（列表首包 `with_meta=1`）到达前搜索卡片只有按钮行（56px），到达后叠加字段行（+50px）把下方表格/分页推移；②el-table 列宽在 rAF 内才落位（EP `requestAnimationFrame(doLayout)`），列挂载后第一帧是浏览器均分宽度（长表头换行 155px → 41px）。修复（`RePlusPage`）：搜索卡片 `min-height` 占位 + 列集合变化时隐藏表格两帧（`visibility: hidden` 不参与 layout-shift 统计）；定位时看 `cls_top` 的 `[dx,dy,宽×高→宽×高]` 几何明细（2026-09-18 收口，0.2403 → 0.0214）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `page.request` 手写 API 路径带**尾斜杠** → 后端 `SimpleRouter(False)`（无尾斜杠路由）返回 **Django 404 HTML 页**，`resp.json()` 报 `SyntaxError: Unexpected token '<'`（易误导为「代理没转发/请求没到后端」）                                                                                                                                                     | 手写路径与前端 `api` 定义逐字对齐：**无尾斜杠**（列表 `/api/system/xxx?size=100`、详情 `/api/system/xxx/{pk}`）；带 cookie 的同源请求用 `FRONT_URL` 前缀（跨域直连 `BACKEND_URL` 不带 cookie，需显式 Bearer）；参考 `approval.e2e.ts` 既有范式（2026-09-16 suggest 实测）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 写数据类用例 + 后端业务唯一/重叠校验（如审批委托「同一委托人时间重叠即拒绝」）+ 固定数据 + 双浏览器共享库/可重复运行 → 后跑者保存被拒（弹窗停留在保存态，`not.toBeVisible` 断言失败，报错具有误导性）                                                                                                                                                             | ① **按 project 隔离数据**（`test.info().project.name === "webkit" ? "e2e_member" : "e2e_user"`，确定性隔离）；② 用例开头 **API 幂等清理**目标数据（列表 + DELETE，使重复运行安全）；③ 断言用变量贯穿（2026-09-16 suggest 实测：双浏览器 + `--repeat-each=2` 各两遍共 4 次全绿）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 并行分片共用桩 LLM 端口（`playwright.config.ts` 的 `E2E_STUB_LLM_PORT` 默认 18897，webServer 会真的 bind 该端口）→ 后起的 shard `EADDRINUSE` 直接退出（报 `Process from config.webServer was not able to start. Exit code: 1`），表现为「随机若干 shard 启动即失败」而与用例无关（2026-09-17 实测 3 路挂 2 路，只剩 1 路在跑）                                    | `e2e-parallel.mjs` 按路分配 `E2E_STUB_LLM_PORT` 与 `E2E_STUB_LLM_URL`（步长 2，与偶数段的后端端口错开），并纳入起止两轮端口清理；**新增任何「会 bind 端口」的 webServer 时，须同步并行脚本的按路分配清单**（同类历史问题：`test-results` 目录共用）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 登录页一次性临时 Token（`/api/system/auth/token`）竞态：提交时 token 尚未返回（空 token），或同页二次登录（登出后组件复用）沿用已被消费的旧 token → 后端 400「临时Token校验失败」，`login()` 30s 停在 `#/login`（隔离复跑稳定复现，非负载；后端日志 `BasicLoginAPIView ERROR: 临时Token校验失败`）（2026-09-17）                                                  | 产品修复：`Basic.vue` 的 `onLogin` 提交前 `await initToken()` 统一刷新（`initToken` 改 async），确保 token 与本次提交一一对应                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| oauth 用例「读当前值 → 清空 → 还原当前值」的还原模式：清空后一旦中途失败（登录/断言超时），重试读到的「原值」已是空数组 → 空配置永久残留，`E2E飞书` 行不可见 / `bind-authorize` 非 1000 连锁失败（重试无法自愈）（2026-09-17）                                                                                                                                    | 还原写**固定种子值**常量（与 `e2e_seed.py` 对齐）+ `try/finally` + `test.afterEach` 接口兜底（幂等）；断言登录页休眠改用**匿名 context**，去掉「登出 → 重新登录」链路的超时面                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 站点配置 `Locale` 是跨用例共享状态：locale 用例失败（或两个防抖 PATCH 乱序）把英文留给后续 spec → 中文定位器（`退出系统` / `展开` / 菜单名）成批失配，**双浏览器、跨模块同时失败**，极易误判为 webkit 负载 flaky（2026-09-17 实测 oauth / zz-preview-smoke 全挂）                                                                                                 | 三层防线：① `waitLocaleSaved` 按**目标语言**精确匹配保存 PATCH（等「任意 PATCH」会误判落库 → reload 后仍中文）；② `afterEach` 接口兜底写 zh（失败重试一次）；③ `helpers.login` 语言自愈：登录后检测非中文 → 写回 zh + reload，保证任何 spec 起点干净                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `openList` 固定 5s 窗口等搜索输入框 → 高负载下把「未折叠但渲染慢」误判为折叠 → 点击不存在的「展开」按钮 10s 超时（双浏览器同挂）                                                                                                                                                                                                                                  | 同时等「输入框可见」与「展开按钮可见」任一出现，再按输入框可见性决定是否点展开；展开点击失败不致命（后续可见性断言兜底）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 并行分片未向子进程注入 `E2E_PARALLEL` → `helpers.HIGH_LOAD` 恒为 false（除非用户手动设置）→ `test.slow()` / `DOWNLOAD_TIMEOUT=90s` 高负载档失效，分片下沿用严格上限随机超时                                                                                                                                                                                       | `e2e-parallel.mjs` 子进程 env 注入 `E2E_PARALLEL=<total>`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `zz-preview-smoke` 并行跑批连挂 5 轮（chromium+webkit 同挂、retry 同挂），隔离复跑必绿——根因是**并行负载首屏的响应式宽度锁死**：用户管理页表格只剩 ID/操作列、搜索字段收起、「请输入用户名」输入框持续 hidden（视口 1280 正常；视口 +1px 重测自愈无效），隔离跑批布局正常（2026-09-17 定位，失败截图为证）                                                        | 用例改「目标行双通道」：分片小库按 **ID 列精确定位**直接取行（绕开搜索区），共享库被挤出首页才回退 `openList` 搜索（helper 保留循环自愈）。教训：① 以「首屏测量」为准的响应式布局在慢加载下会锁死错误宽度（产品侧候选：ResizeObserver 复测）；② 用例定位优先选不依赖响应式中间态的稳定列（如 ID），搜索通道只作大库回退                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

| 自研页面在 RePlusPage 操作列**新增按钮**后，既有按钮被挤进「更多」下拉（`showNumber` 未显式设置时按默认折叠）→ `getByRole('button', { name: '编辑' })` 恒超时、报「waiting for …」而不是「元素不存在」，极易误判为页面渲染失败（2026-09-18 表单设计器加「存为模板」实测） |
| 新增按钮文案**包含既有按钮名**（「保存草稿」含「保存」）→ 既有用例 `getByRole('button', { name: '保存' })` strict mode violation（解析出 2 个元素，dform/dform-flow 同挂）；处置=新按钮文案避开子串冲突（改「存为草稿」），或既有断言补 `exact: true`（2026-09-18） |
| `.el-dialog` / `.el-drawer` 的 `.last()` 是**惰性求值**：子弹窗关闭后 `.last()` 重新绑定到父弹窗 → `await expect(propsDialog).not.toBeVisible()` 必失败（父弹窗仍在）；处置=断言锚定子弹窗内的唯一 testid（如 `field-prop-placeholder`），不要用 `.last()` 做「已关闭」断言（2026-09-18 字段属性弹窗实测） |
| `el-select` 自身的 readonly input 与 `el-input` 的输入框混在 `locator("input")` 序号里 → `.nth(1).fill()` 拿到选择器输入框，报「waiting for element to be visible, enabled and editable」超时；处置=先过滤 `input:not([readonly])` 再按序号取（2026-09-18 分支路由条件行实测） |
| 用户管理页行操作收敛进「管理」抽屉后，原先依赖「更多」下拉的定位全部失效（`change-history` / `mfa` / `preview` / `zz-preview-smoke`）：操作列只留编辑 / 删除 / 管理，其余动作（重置密码 / 重置MFA / 强制下线 / 分配角色 / 权限预览 / 邀请 / 发送通知 / IM 绑定 / 打标签 / 变更历史）在抽屉内按分组渲染 | 统一走 `helpers.openUserPanel(page, row)`（点「管理」→ 返回抽屉 locator）+ `clickUserAction(drawer, code)`（按 `data-action-code` 定位，与文案解耦）；新增用例见 `user-panel.e2e.ts`（2026-09-24） |
| 双击分隔条重置分栏时，第二次 click 落在重排后的表格数据列上：命中用户名列（抽屉入口）会误开抽屉，其遮罩吞掉后续分隔条拖拽（`elementFromPoint` 命中 `.el-overlay`，表现为左栏宽度停在默认值、用例断言 35~55 全失败） | 用例在 `dblclick` 后显式兜底：若存在可见 `.el-drawer` 则 `Escape` 关闭并等其 hidden（`split-pane.e2e.ts`）；同类「点击落点漂移后命中交互元素」的场景都按此模式收口 |
| 经 API 建用户时密码返回 400「密码不满足安全规则」，换同长度同复杂度的密码即可通过：POST 分支会先把提交值按密文协议解密，明文若恰好被 base64 解码成空串 → 明文规则校验必失败；只有解密抛异常的密码才会回退明文（后端日志 `create user and set password failed: … so set default password`） | 选密码前本地验证：`DJANGO_SETTINGS_MODULE=tests.settings_e2e python -c "…AESCipherV2(user).decrypt(pwd)"` 必须抛异常（`user-panel.e2e.ts` 的 `P@ssw0rd-E2EPanel-2026` 已验证；`E2E-Hist-2026!` 亦可） |
| a11y `aria-required-parent` 出现新节点形态 `.is-active.el-menu-item[role=menuitem]`（当前激活菜单项）：axe 对该节点取「最短唯一选择器」，不带 `a[href$=…] >` / `.nest-menu` 前缀，既有豁免模式未覆盖 → 用户管理页用例稳定失败 | 先判性质再补豁免：临时 axe 探针打印节点 `html` 与 `data-insp-path`（本次全部命中 `lay-sidebar` 组件，页面内容区零违规）→ 补 `ALLOWED_VIOLATIONS` 模式并登记 `docs/accessibility-audit.md`（2026-09-24） |
| EP 2.14 的 `el-select` 选中值渲染为**文本节点**（filterable 模式下 input 不承载 label）→ 对选中值用 `toHaveValue` 断言恒失败；处置=对 select 容器用 `toContainText`（2026-09-18 仪表盘设置重命名实测） |
| 表单模板场景：副本名 = 模板名 + 后缀，`getByRole('row', { name: templateName })` 是**子串匹配**→ 副本行被命中，`toHaveCount(0)`（断言模板不在表单列表）必失败；处置=断言名避免子串包含关系，或断言正向存在性而非数量（2026-09-18 模板用例实测） |
| 表单草稿 × 操作审批：审批通过后的**重放必须先于草稿态检查**（自动落库后状态已不是 DRAFT，否则重放被「仅草稿可提交」拒绝）、且通过后动作按 `approval.object_pk` 更新既有行（不重复建行）——协议顺序由 `test_dform_draft_template.py::test_draft_submit_then_auto_complete_and_replay` 守护（2026-09-18） |

| 自研功能给审批人/管理员发通知（催办/审批提醒）后，共享库里管理员出现未读角标（`.el-badge__content--danger`）→ a11y 扫描新增 color-contrast 节点成批失败；**全新种子（test:e2e:fresh）下 a11y 字母序靠前先跑、不受影响**，非 fresh 的复跑才会命中（2026-09-18 实测：污染库双浏览器挂、fresh 全绿） |
| split-pane 拖拽「分隔条不动、表格文本被划选」（左栏停在默认 20%）= 首轮列表的 **el-loading 遮罩未退场**（遮罩覆盖整个分栏容器含分隔条，mouse 事件被吞）；拖拽/点击前先等「有数据行 + `.el-loading-mask` 计数为 0」。此前登记的「webkit 位移判定限制」实为同一遮罩竞态，加等待后 webkit 也稳定通过（2026-09-18 实证，steps 等拖拽参数无需改动） |
| RePlusPage 自适应高度在「上方内容过多 + 矮视口」下会算出不可用小高度（AI 配置页两卡在上，表顶 577 → 表高 33px、表体 0 高、行溢出到分页之下、按钮滚入视口后被 sticky 表头拦截）；已在组件内钳制最小 260px（写定值只依赖表顶位置，不与 RO 振荡），超出部分页面级滚动兜底（2026-09-18 修复 ai.e2e webkit 6/6 失败） |
| EP 2.14 的 el-loading 遮罩在 fade-leave 期间仍拦截点击（webkit 下更久）→ 「行已可见就点按钮」会撞 `el-loading-spinner intercepts pointer events`；处置=同上等遮罩计数为 0，不要靠 retries 硬扛（2026-09-18） |

| split-pane 用例把拖拽结果**持久化到服务端**（WEB_SITE_CONFIG.SplitPanes 跨会话生效）→ 泄漏给共享库后续用例：左栏 60% 把分栏页右侧压扁（搜索输入框宽度 0 → fill「element is not visible」，import-mapping/preview 双浏览器连挂）；处置=用例 afterEach 统一 PATCH 回默认 20（勿回写已污染的历史「原值」）；同批 chromium 跑得快时会在 webkit 还没跑 split-pane 前就污染它（2026-09-18 实测） |
| `el-dropdown` 的 popper 在 DOM 里保留多份隐藏副本（`getByText("趋势数据（CSV）")` 一次命中 3 个 li）→ strict mode violation 双浏览器同挂（2026-09-19 监控页导出下拉实测） | 下拉项断言必须限定可见菜单：`page.locator(".el-dropdown-menu:visible")`（同类：EP 多实例弹层断言一律先过滤可见态） |
| 监控页趋势/告警数据依赖 `common.Monitor` 心跳表，而 E2E 后端是 daphne（**不跑 gunicorn 的启动心跳线程**）→ 历史趋势恒空态、图表用例无从断言（2026-09-19） | `scripts/e2e_seed.py` 增 `seed_monitor_scene()`：造近 24h 心跳（5 分钟粒度，网络累计量递增使速率可算）+ 一条 firing 告警；断言图表 `[data-testid="monitor-history-chart"] svg` 与告警记录行 |
| 影响面 / 错误定位 E2E 三坑（2026-09-22）：① `openMenuPath` 目录层级必须与真实菜单一致（角色页在「系统管理 → 权限管理」下，只传一级会 `element is not visible`）；② 申请人视角看不到自己发起的单在「待我审批」页签（敏感操作审批在「我发起的」、流程实例在「我的申请」）；③ `Date.now()` 参与日期派生禁位运算（超 32 位截断成负数 → 非法日期 400） | 三处均已修正：按实际层级导航、按 `object_pk` / `biz_id` 精确定位并切对页签、日期用 `Date.UTC` 游标递增（并加实例取消兜底清理） |
| approval-chain「初审驳回终止」用例在 batch / 隔离 / fresh 三种运行下均失败（2026-09-22 发现）：失败点为 pageB（第二超管）待我审批列表按单号找行超时。`--trace on` 证据为「列表 API 已返回该单（total=1、PENDING、object_pk 命中）但 DOM 未见该行」 | **2026-09-23 定位并修复**：根因是「审批列表不做实时推送」——第二个单（单 B）提交后，pageB 停留在单 A 驳回后已渲染的旧列表，WS 不会触发列表刷新，所以新单行永远不出现（单 A 能过是因为 pageB 刚导航、列表是新拉的）。修复=在 pageB 等待单 B 行前显式 `reload()` + `openMenuPath` 重新导航拉取最新待办（并与 `waitNotificationsGone` 配合避免通知遮挡点击）。双浏览器 2 passed |
| 页面侧手动「注入搜索条件 + 刷新」（跨页跳转带参）：与 RePlusPage 首开请求（`with_meta=1` 内联元数据）产生**请求序号竞态** —— 二次请求把首开响应判为过期丢弃，元数据永久缺失（表格无列 + alert「未获取到列元数据」，但分页 total 正常、数据已过滤，极易误判为后端元数据故障）（2026-09-23 实测） | 跨页跳转**只需 `?key=value`**：RePlusPage 自动把 `route.query` 装配进搜索默认值并二次请求（`usePlusPageData.fieldsInitCallback`）；页面不要再手动注入 + `handleGetData()`。判据：首开后缺元数据但 total 正确 → 查是否有页面侧二次刷新抢跑 |
| el-table 排序 caret 的精确点击：`getByRole("button", { name: "按 X 排序" })` 的可访问名按钮命中点在两个 caret 之间，Playwright 点击落在 descending 箭头上 → 断言升序请求 `ordering=field` 却收到 `-field`（2026-09-23 U-3 E2E 实测） | 排序方向必须显式定位：`headerCell.locator(".sort-caret.ascending")` / `.sort-caret.descending` 分别点击；另注意列展示名可能与元数据 label 不同（`date_joined` 表头显示「注册时间」而 search-columns label 为「加入日期」），断言用真实表头文案 |
| 动态路由组件解析「子串包含取首个命中」劫持页面（2026-09-23）：菜单 `component` 为目录片段（如 `settings/message`）时，页面目录下新增任意文件（如 `settings/message/components/MessageTemplatePanel.vue`，字典序在 `index.vue` 之前）会被优先命中 → 整页渲染成该子组件，**无 console 报错、无白屏**，仅表现为「页签/表单全消失」（消息设置页实测，notify-im 用例连续失败并被误判为「页面形态变化」） | 解析改**精确优先**（`base` → `base.vue` → `base/index.vue`，最后才退化子串兜底），纯函数 `src/router/utils/resolve-component.ts` + 守护单测；排查判据=渲染内容与预期不符但无报错 → 用 DOM 的 `data-insp-path` 确认实际挂载组件文件 |
| 确认层（popconfirm/message-box）定位命中**隐藏 popper 副本**：`.locator(".el-popconfirm, .el-popper, .el-message-box")` 取 `.first()` 可能点到常驻隐藏下拉的「确定」→ 用例偶发失败（approval-chain「通过」步骤实测） | 收敛到可见态 `.locator(".el-popconfirm:visible, .el-message-box:visible")`，或按文案 `.filter({ hasText: "撤回" })`（两种写法均已验证通过） |
| 审批动作后的状态断言存在**落库竞态**：点击撤回/通过确认后立刻 `page.request` 查询会读到旧状态（PENDING），表现为「撤回失败」的偶发失败 | 状态断言改 `expect.poll(...)` 轮询至终态（15s）后再校验其余字段（如 `current_level` 归零） |
| 审批轨迹「处理人」已改为**显示名快照**（昵称优先、缺失回落 username），「代理来源」留痕仍保留 username | 断言显示名用 `PLAIN_USER.nickname`（helpers 已登记昵称）；不要断言轨迹出现处理人 username（代理标注除外） |
| 列首帧隐藏窗口固定两帧在首屏任务多的页面不够用：用户管理页在行操作抽屉上线后实测（dev 链路稳定复现，把用户名改回纯文本渲染同样复现 → 与具体列渲染无关），列宽落位被推到第 2 帧之后，`cls_top` 出现 `tr` 行高 86→53 + 表体高度回弹，CLS 0.02 → 0.25 | `useTableLayout` 抽 `LAYOUT_HOLD_FRAMES = 4`（实测恢复到基线，代价多隐藏约 33ms）并补 `useTableLayout.spec.ts` 逐帧守护；判断手法=看 `cls_top` 几何明细里 `el-table__body-wrapper` 高度回弹 + `tr` 行高突变即该窗口不足 |
| 行操作抽屉化后定位口径变更（2026-09-24，用户/API 应用/知识库/AI 档案四页）：行内入口为「管理」（知识库为「预览」），抽屉内动作按 `data-action-code` 定位；原「更多」下拉路径全部失效（既有 spec 已更新） | 统一走 `helpers.openEntityPanel(page, row, name?)` + `helpers.clickPanelAction(panel, code)`（用户页 `openUserPanel` / `clickUserAction` 为同义封装）；抽屉内「先收起再开二级弹层」的动作，其后续断言要等抽屉关闭 |
| 浮层内再嵌套浮层（「我的视图」面板 el-popover 内放行操作 el-dropdown）会因外部点击判定互相关闭：点内层 popper 被视为落到 popover 外部 → 面板先关、内层随之销毁，操作点不到（2026-09-24 设计期规避） | 面板内行操作**平铺为文字按钮**（不引入第二层 popper）：应用=点行、星标切换默认、更新/重命名/另存/删除为行内按钮；E2E 按 `data-testid`（sv-panel / sv-item / sv-save / sv-update）定位 |
| `pnpm test:e2e:fresh -- e2e/x.e2e.ts` 的白名单参数实测未生效（跑了全量 434 例），定向跑被拉长到小时级 | 定向跑用 `pnpm test:e2e -- e2e/x.e2e.ts`，或先 `kill` 18896/18897/8848 后 `npx playwright test e2e/x.e2e.ts`（本次采用后者）；**跑批前确认 18897（桩 LLM）端口空闲**，否则 webServer 启动即报 `already used` 直接退出 |
| locales 词条重复定义 → vite 报 `Internal server error: Map keys must be unique`，locale 模块整块加载失败：**页面全白、整批 E2E 全在 `login()` 填账号处 30s 超时**（与用例无关，极易误判为环境/后端故障）| `scripts/check-i18n-keys.mjs` 已加「同父路径重复键」检测（含行号与首次出现行号，已实测可拦）；排查特征=playwright 日志里 `[WebServer] ... Map keys must be unique` + 全部用例挂在登录阶段 |
| 三级菜单漏传中间层（角色管理为「系统管理 → 权限管理 → 角色管理」）会让导航超时：`openMenuPath(["系统管理"], "/system/role/index")` 里目标 link 始终不可见（位于未展开的子目录内），外层自愈循环反复重开父目录，最后一次点击把「系统管理」收起，最终报 `link.click` 10s 超时（直觉上会误判为「目录点不开」） | dirs 必须传完整链路（`["系统管理", "权限管理"]`）；按文本定位节点自身用 `.el-tree-node__content`，`.el-tree-node` 会命中最外层祖先节点 |
